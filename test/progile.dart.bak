import 'package:flutter/material.dart';

class SelectTap extends StatelessWidget {
  const SelectTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 30),
      child: DefaultTabController(
        length: 6,
        child: Column(
          children: [
            const TabBar(
              isScrollable: true,
              indicatorColor: Colors.white,
              labelColor: Colors.white,
              unselectedLabelColor: Colors.grey,
              tabs: [
                Tab(text: '코딩'),
                Tab(text: '생명주기'),
                Tab(text: '상태관리'),
                Tab(text: '애니메이션'),
                Tab(text: '협업툴'),
                Tab(text: '사진편집'),
              ],
            ),
            SizedBox(
              height: 300,
              child: TabBarView(
                children: [
                  Center(
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        RichText(
                          textAlign: TextAlign.center,
                          text: const TextSpan(
                            children: [
                              TextSpan(
                                text: '2만줄 넘는 코딩시간. ',
                                style: TextStyle(
                                  color: Colors.white,
                                  fontSize: 14,
                                ),
                              ),
                              TextSpan(
                                text:
                                    '회사와 함께 성장하기에 적절한 코딩시간입니다.\n'
                                    '각종 시행작오가 있었던 과거의 시간들\n\n'
                                    '불가능이란 없고 뭐든 할 수 있다는 마음을 가지게 해준 2만줄의 경험,\n'
                                    '이제 이 경험을 활용할 시간이 되었습니다.',
                                style: TextStyle(
                                  color: Colors.grey,
                                  fontSize: 14,
                                ),
                              ),
                            ],
                          ),
                        ),
                      ],
                    ),
                  ),
                  const Center(child: Text('생명주기')),
                  const Center(child: Text('상태관리')),
                  const Center(child: Text('애니메이션')),
                  const Center(child: Text('협업툴')),
                  const Center(child: Text('사진편집')),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
