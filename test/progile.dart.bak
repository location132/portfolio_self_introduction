child: ClipRRect(
              borderRadius: BorderRadius.circular(10),
              child: Stack(
                alignment: Alignment.center,
                children: [
                  AnimatedOpacity(
                    opacity: widget.scrollCount == 9 ? 1 : 0,
                    duration: const Duration(milliseconds: 720),
                    child: Image.asset(
                      'assets/images/story_3.png',
                      fit: BoxFit.contain,
                    ),
                  ),
                  Text(
                    '입학 후,\n'
                    ' 개발 강의를 처음 수강했던 날의 기억이 아직도 생생합니다.\n\n'
                    '기본 개념조차 잡히지 않았던 저와 동기들,\n'
                    ' Unix 수업 시간에 처음 들은 cd와 ls.\n'
                    ' 하지만 이게 왜 필요한지, 어디에 쓰이는 건지 몰랐던 우리.\n\n'
                    '궁금증만 커져가던 시간 속에서,\n'
                    ' 우리는 서로를 붙잡고, 매일 스터디에 모였습니다.',
                    style: GoogleFonts.notoSans(
                      fontSize: 18,
                      fontWeight: FontWeight.w600,
                      color: Colors.black,
                      height: 1.5,
                    ),
                    textAlign: TextAlign.left,
                  ),
                  AnimatedOpacity(
                    opacity: widget.scrollCount == 10 ? 1 : 0,
                    duration: const Duration(milliseconds: 720),
                    child: Image.asset(
                      'assets/images/story_1.png',
                      fit: BoxFit.contain,
                    ),
                  ),
                  // 이 부분을 완전히 다른 이미지라 생각
                  AnimatedOpacity(
                    opacity: widget.scrollCount == 11 ? 1 : 0,
                    duration: const Duration(milliseconds: 720),
                    child: Image.asset(
                      'assets/images/story_2.png',
                      fit: BoxFit.contain,
                    ),
                  ),

                  //-------------
                ],
              ),
            ),


              @override
  void initState() {
    super.initState();
    _chapter2Animation = Chapter2Animation(vsync: this);
    print("checkTextPart ==> 자식 애니메이션 인스턴스 : ${_chapter2Animation.hashCode}");
    _chapter2Animation.animationWithTextForward();
  }